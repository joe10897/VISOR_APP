<?xml version='1.0' encoding='utf-8'?>
<widget id="com.visor.app" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0" xmlns:android="http://schemas.android.com/apk/res/android">
    <name>VISOR System</name>
    <description>
        Simon Wong
    </description>
    <author email="842679135jjj@gmail.com" href="https://cordova.apache.org">
        Apache Cordova Team
    </author>
    <content src="index.html" />
    
    <!-- 允許所有跨域請求 -->
    <access origin="*" />
    
    <!-- 全螢幕設定 (啟動時隱藏狀態列) -->
    <preference name="Fullscreen" value="true" />
    
    <!-- 允許開啟的外部連結類型 -->
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="tel:*" />
    <allow-intent href="sms:*" />
    <allow-intent href="mailto:*" />
    <allow-intent href="geo:*" />
    <allow-intent href="ws://*/*" />
    <allow-intent href="wss://*/*" />

    <!-- 關鍵修正：解決 HTTPS 頁面無法連線 ws:// 的問題 -->
    <preference name="scheme" value="http" />
    <preference name="hostname" value="localhost" />
    <preference name="MixedContentMode" value="0" /> 

    <platform name="android">
        <allow-intent href="market:*" />

        <!-- 【關鍵修正】開啟明文傳輸權限，否則無法連線到 Pi 5 (http/ws) -->
        <edit-config file="app/src/main/AndroidManifest.xml" mode="merge" target="/manifest/application">
            <application android:usesCleartextTraffic="true" />
        </edit-config>

        <!-- 統一管理所有權限 (使用 config-file 插入 manifest) -->
        <config-file parent="/manifest" target="AndroidManifest.xml">
            <!-- SMS 簡訊發送 -->
            <uses-permission android:name="android.permission.SEND_SMS" />
            
            <!-- GPS 精確定位 -->
            <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
            <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
            
            <!-- 藍芽權限 (Android 12+ 必備) -->
            <uses-permission android:name="android.permission.BLUETOOTH" />
            <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
            <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
            <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
            
            <!-- 網路狀態檢查 -->
            <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
            <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
        </config-file>
    </platform>
</widget>